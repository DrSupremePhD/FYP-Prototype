{
  "name": "privagene-backend-db",
  "version": "1.0.0",
  "description": "Modular SQLite + file storage service for PrivaGene (drop-in for ZIP submission)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "node -e \"require('./db/db').runMigrations(true)\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "sqlite3": "^5.1.6",
    "uuid": "^9.0.1"
  }
}
